 Imagine you're talking to, like, the smartest person in the world. I mean, they write every book, they know every scientific theory, they can even write poetry in a dozen languages. Right. Sounds like a pretty great conversation. It does, but there is a massive catch here. Yeah. Every single time you finish a sentence, their memory wipes completely clean. Oh, wow. Okay. Yeah. So to keep the conversation going, you literally have to constantly remind them who you are, what you just asked, and what they just answered. I mean, it sounds exhausting, right? It honestly sounds like a psychological thriller or something. Exactly. But I mean, you've just perfectly described what it's like interacting with a standard, large language model straight out of the box. Welcome to The Deep Dive. Today, we are looking at this incredibly dense information pack document we've got. Titled, genieichatbot and rag, quick revision notes dot pdf. It's a fantastic set of notes. It really is. Yeah. And our mission today is to completely demystify how modern AI chat bots work under the hood. And more importantly, how they get upgraded from these simple amnesiac conversationalists into highly accurate, custom data powerhouses. Which is such a crucial shift in the industry right now. Right. Okay, let's unpack this. Yeah. Because whether you are prepping for a tech interview, or you were just intensely curious about the AI tools you use every single day, we're going to tear down exactly how these systems operate. Yeah, we'll get right into the plumbing of it all. Exactly. So let's start the absolute foundation. Before we can build this advanced custom AI, we have to understand that basic plumbing of a standard chatbot application. Well, the baseline architecture is actually quite straightforward. You basically have the user input, which is handled by a front-end framework. Like a streamlit. Yes, exactly. Streamlit is heavily used in the Python ecosystem for this. So that front-end application authenticates with a provider using an API key. Got it. And then it fires your prompt over a network to an LLM, like Gemini or OpenAI. Then the model generates a text response that gets routed back to your screen. Okay, but this brings us right back to that major flaw we talked about at the start, the amnesia. The model by default just instantly forgets previous messages. Right. You say, hi, my name is Alex. It says hello, Alex. Your very next prompt is, what is my name? And it just draws a complete blank. Yeah, the classic goldfish memory problem. Goldfish memory problem. I love that. It's very real. And this happens because interactions with these AI models at the API level are entirely stateless. Stateless meaning it doesn't hold on to any history. Precisely. For the sake of global scalability, you know, the provider's server does not retain a running memory of your session. It basically treats every single API request as a completely isolated universe. Which makes sense for server load, I guess. But it makes having an actual flowing conversation impossible. So if the LLM refuses to maintain state on its end, Howard developers actually getting around this to create the illusion of memory. Well, the front end application has to do all the heavy lifting there. Oh, really? Yeah. In Streamlit, for example, developers utilize a specific state management tool called s.session underscore state dot messages. That's a mouthful. It is, but it essentially acts as a temporary local storage locker. It catches and holds on to every single message sent and received during your active session. Okay, let me make sure I'm tracking the mechanics here. To make the AI remember, this Streamlit work around literally takes your brand new message, pulls out a massive transcript of everything you have both ever said in that specific chat window. Yep. Stables your new message to the bottom and passes that entire giant manuscript back to the LLM over the network. That is the exact mechanism. Yeah. You are sending the entire cumulative conversation history back and forth with every single new prompt. Wow. Just so the LLM has the necessary context to generate a relevant response. I mean, I have to challenge the efficiency of that. If I'm chatting with an AI for an hour to troubleshoot a huge block of code, that transcript is going to get massive. Oh, absolutely. It scales terribly. Right. I'm burning through my context window limit, which means latency is going to spike because the model has to read like thousands of previous words before it even starts typing a new answer. Yes. And on top of the Blooning API token costs, there's another huge issue. What's that? Even with a perfect memory of your current conversation, the AI still only knows it's original training data. Oh, right. So if I ask it about a private internal company document, it has no idea what happens when it lacks custom data. It attempts to be helpful anyway. And that usually results in an AI hallucination. Right. We're just starts making things up. Exactly. Because the architecture is designed to predict the next most likely word in a sequence. If it lacks factual grounding on a private topic, it will just confidently stitch together a plausible sounding but completely fictitious answer. That's so dangerous for business applications. It really is. Which race is an important question? How do we give an AI new private knowledge without spending millions of dollars and months of computing time to retrain the entire foundational model from scratch? And the ultimate solution for this is an acronym I feel like I'm seeing literally everywhere lately. A REG. A REG. Retrieval augmented generation. Yes. Instead of relying purely on the models internal frozen in time training data, we fundamentally alter the application flow to introduce external dynamic information. Wait, how does the flow actually change in practice? Well, we pivot from a simple direct request response model to a multi-step pipeline. Okay. First, the user asks us a question. Uh-huh. Second, the system pauses and it reaches into an external database to retrieve highly relevant factual information. Gotcha. Third, it injects that specific information into the prompt as context. And then finally, fourth, the LLM generates the answer based heavily on that newly provided context. Here's where it gets really interesting. We're essentially giving the AI an external searchable brain to fact check itself before it speaks. That's a great way to put it. Yeah. So if you are listening to this right now and you've ever uploaded your company's Massick HR handbook into a custom GPT and you were amazed that it instantly knew the complex new vacation policy. Right. This external brain is exactly what was happening behind the scenes. It is a brilliant architectural shift. It changes the AI's primary job from remembering facts from its training to reading comprehension. Oh, that makes so much sense. Yeah, it just reads the factual context to dynamically handed it and synthesizes an answer. And this drastically reduces hallucinations because the model is anchored to your specific private data. Okay, I love the theory here, but let's get into the actual mechanics of building this. Say I wanted to build a RAG system to analyze candidate resumes. A resume is a PDF, right? Usually, yes. It's full of weird formatting, bold fonts, columns, maybe a headshot, and LLM cannot just magically ingest a visual PDF file. Mechanically, no, it can't. The model's process plain text. So the very first step in a RRAG pipeline is text extraction. Okay, how does that work? You have to utilize a parsing library. A common one in Python is pipe EF. Right. You use it to programmatically strip away all the visual formatting, ignoring the margins and the fonts, and outputting just a single continuous string of raw text. But if we extract a massive document, say a 100 page technical manual instead of a resume, I can't just shove all that raw text into the AI's prompt at once. We run right back into that token limit issue we just talked about. Exactly. Larger documents easily exceed the context window. And beyond strict limits, feeding an LLM and overly massive document dilutes its attention mechanism. So it gets lost in the weeds. Right. It struggles to find the needle in the haystack. Therefore, the extracted raw text must go through a process called chunking. Chunking? Yes. You programmatically split the text into smaller, mathematically digestible pieces. I always thought of chunking like trying to eat a massive 72-ounce steak. Oh, boy. You can't just unhinder jaw and swallow a hole. You choke. Exactly. The AI system simply can't swallow and digest a massive document hole. You literally have to cut it up into these bite-sized chunks so the system can actually process them. A very visceral analogy, but highly accurate. In practice, a typical chunk size might be, say, 500 to 1,000 characters. OK. So from the notes, chunk three might specifically be the project section of a resume. But here is the critical engineering detail. You don't just blindly slice the text every 500 characters. Wait, why not? Just from path purposes. Because if a 500 character boundary falls right in the middle of a sentence, you completely destroy the context of whatever thought was happening. So that makes sense. It would just cut off midword. Exactly. So developers use overlapping chunks. Chunk one might go from character zero to 500. Chunk two might start at character 400 and go to 900. I see. That 100 character overlap acts as a semantic bridge. It ensures that no crucial meaning, like a job title being separate from its employment dates, is lost at the cutoff points. OK. So now I have a bucket full of hundreds of overlapping text chunks floating around in my system. If a user asks the chatbot a question, how does the system know which chunk is the relevant one to pull out? I can't read all of them every single time to check. Right. This is where we cross from basic scripting into the deeper data science of Gen.A.I. We introduce embeddings. Embeddings. Yes. Embeddings are the process of converting human language into high-dimensional numerical vectors. Wait, you're saying it turns a phrase like Python developer into a coordinate, like a latitude and one is you done a map? Kind of. Yeah. How does a math formula know what a developer is? What's fascinating here is it actually doesn't know what a developer is, at least not the way human cognition works. OK. When you run text through an embedding model like a Gemini embedding 001, it maps the semantic meaning based on the frequency of how those words appear next to other words across its vast training data. Oh, well. Yeah. And it outputs an array of floating point numbers. So Python developer becomes something like bracket 0.23, negative 0.4, 0.91, and so on. And the whole purpose of this translation into numbers is to establish spatial relationships. Precisely spatial relationships, yes. But in a multi-dimensional vector space, some models mapped concepts across like 768 different dimensions. I heard it's vibrating a little. It's intense math. But the core mathematical principle is simply that similar texts end up having similar numerical vectors. OK. Let me see if I'm tracking this. Python developer and software engineer will output numbers that are mathematically very close to each other in this space. Yes. But Python developer and like blueberry pancake will be mathematically miles apart. You've nailed the concept perfectly. We measure that closeness using mathematical formulas. Often something called cosine similarity, which calculates the angle between two vectors. The smaller the angle, the closer the meaning. And it's super powerful because it captures intent, not just keywords. It knows that university diploma and college degree mean the same thing even though they share literally zero words. That is wild. But to calculate that map instantly across thousands of chunks, you need to specialize architecture. You need a vector database. Right. Like chroma DB or pine cone or weeve 8. Exactly. Or ASS, which stands for Facebook AI similarity search. Right. The notes highlighted ASS. ASS is incredibly popular for local infrastructure. And the key architectural detail of a vector database is that it doesn't just store the map. It stores the mathematical vector paired with the original text chunk. I picture a vector database like a giant magical library. Okay. I like where this is going. Instead of the books being sorted alphabetically by the author's last name, they are sorted by vibes vibes. Vibes yes. They're sorted by pure semantic meaning. So when I walk into this library and say I need something about coding, I don't have to wander every aisle. The database architecture instantly magnetically pulls me to the exact geographic corner where all the coding via paragraphs live. That magnetic pull is the similarity search step. When the user types a question into the chatbot, that specific question is immediately converted into its own embedding vector using the exact same embedding model. Oh, that makes total sense. You have to translate the user's question into the same language of numbers so they can actually be compared. Correct. The vector database calculates the cosine similarity between your questions vector and every single chunk vector stored in its memory. And them. It mathematically ranks them and retrieves the top most relevant chunks, say the candidate's education chunk and their skills chunk, while completely ignoring the irrelevant hobbies chunk. Okay. So the retriever went into the magical library, found the three most relevant paragraphs and brought them back. But how does it hand those paragraphs over to the LLM? What does that final payload actually look like? It requires dynamic prompt construction. This is honestly an engineering art form. The application builds a hidden prompt behind the scenes before sending it to the model. What's in the prompt? It takes the retrieved text chunks, for example, project, customer turn prediction, and skills, Python, SQL. It pairs that context with the user's original question, like what projects has the candidate worked on? And then it slaps a massive guardrail on the LLM. Yes. The final prompt, it commands the model with extreme prejudice, answer using only the context below. Only. That's the ultimate hallucination cure right there. You are forcefully restricting this highly creative, expansive AI brain. Exactly. You are basically telling it, ignore your vast knowledge of the entire internet. Act exclusively as a summarizer for these three specific paragraphs I am handing you. Mechanically, yes, you are completely constraining the generation. Actually, let's do a rapid fire trace of this entire R.A.G architecture pipeline from start to finish just to solidify the flow for everyone listening. I love it. Let's do it. I'll kick it off. We start with the raw document, a resume PDF. We utilize a parsing library to extract the plain text. We slice that text up into overlapping 500 character chunks. We pass those chunks through an embedding model to generate numerical vectors. We store both the mathematical vectors and the plain text inside of vector database like AISS. Then the user types a question into the streamlet front end. We embed the user's question into its own vector. Faye Asus performs a similarity search, calculating the math to find the closest chunk vectors. It retrieves the plain text of those top ranking chunks. We construct a strict prompt to combine the retrieve text, the user's question, and the command to only use the provided context. We send that highly specific payload over the API to the Gemini LLM. And the LLM generates a precise custom data response. Oh. We did it. A fully functioning AISS architecture. We did. It's a highly elegant pipeline. It really is. But building this incredible machine locally on your laptop is a great learning exercise, but it doesn't help anyone else use it. True. If we want this to be a real enterprise application, we have to deploy it to the cloud. Yes. And deploying an LLM application introduces a whole new layer of architectural complexity, particularly around environment management. OK. If you are using a hosting platform like Streamlit Cloud, the server starts as a completely blank slate. It needs a blueprint of exactly what Python libraries your code depends on, like, poply F, the face, ASP library, and the LLM SDKs. Right. Because of the classic, it works on my machine syndrome. Yeah. You have to perfectly mirror your local development environment on the cloud server so the app doesn't just crash on launch. Exactly. Which is handled by managing your dependencies file. You'd use a command like pip-free is greater than requirements.txt. But the much larger, frankly, more dangerous architectural challenge during deployment is API key management. Oh, yeah. This is a massive red flag for me. If my application relies on a Gemini or OpenAI API key to function, and I'm pushing my application code to a public or even private GitHub repository to trigger that cloud deployment, yeah, I absolutely cannot hard code my API key into my Python script. If a bot scripts GitHub and finds my key sitting in plain text, someone is going to spin up a server farm and rack up a massive API bill on my credit card by morning. Security is paramount here. You never commit secrets to version control. Modern cloud deployment platforms handle this via secrets management. How does that work? Instead of putting the key directly in the code, you inject it securely into the environment variables of the hosting platform itself. You set something like Google LappiKey equals your actual key in the platform settings. So the key lives securely on their encrypted servers, completely separated from your GitHub code repository. Correct. And your code simply requests the key at runtime from the host's security dictionary using something like rest.dot secrets. It keeps your credentials completely safe from scrapers while allowing the deployed web application to authenticate and function seamlessly. So what does this all mean? To me, when you look at how streamlined this deployment architecture is, it really highlights just how accessible these incredibly powerful tools have become. Oh, completely. With a solid understanding of embeddings, a vector database, and secure cloud deployment, a single developer can literally build a custom enterprise grade AI tool in an afternoon. It is remarkably democratic. If we connect this to the bigger picture, to really cement this system in your mind, especially if you're fielding architecture questions in a Genai interview, we can still rag down to an elegant mental model. Lay it on me. In a basic chatbot, it's just a direct line, the user to the LLM to the response. But in a rag system, you insert a cognitive middleman. Okay. The user talks to the retriever. The retriever fetches the relevant context, feeds it to the LLM, and you get the response. So we can basically map these technical components to human cognition. Let's hear it. The LLM is the brain. The embeddings represent the understanding of language. The vector database is the long-term memory, and the retriever is the search mechanism. Beautifully summarized, RageSimple equals Brain Plus Memory. Brain Plus Memory. Thank you for acting as such an incredible guide through this architecture today. We journeyed all the way from a forgetful, hallucinating basic chatbot through the multi-dimensional math of embeddings to arrive at a highly precise, secure, Rage Pipeline. Anytime. By giving the AI a dedicated searchable memory, we really force it to ground its intelligence and reality. But as we wrap up today's deep dive, I want you to mull over a somewhat chilling thought. We just established that in a Rage System, the final prompt strictly commands the AI answer using only the context below. Right. It relies entirely on the text chunks retrieved by that vector database. But what happens to the AI's truth? If just one of those retrieved chunks contains subtly corrupted biased or purposely poisoned data, because if the AI is mathematically locked into only trusting the context it has fed, its massive brilliant brain is completely helplessly at the mercy of whatever it finds in its memory. It's a systemic vulnerability that builders and all of us as users will have to navigate very carefully in the future, something to think about.